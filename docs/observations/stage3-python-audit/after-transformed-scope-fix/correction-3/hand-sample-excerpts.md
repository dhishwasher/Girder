# Hand-read excerpts: original 20-site sample plus the 8 previously-unresolved targets
Generated by `generate_excerpts.py` (this directory) -- every site listed here
is read directly from the extracted package source (rows N-3..N+3 around the
call site's own row), not summarized or paraphrased by hand. Two groups:

1. The original 20-site sample `correction-1/hand-sample.json` drew, with a
   fixed seed, from the (at-the-time incomplete, 309-claim) population --
   still 20 genuine members of the corrected 347-claim population, just not
   re-randomized against it. Each is re-matched here to its corrected claim
   record and read fresh.
2. One call site for each of the distinct `(file, name)` targets that the
   unpadded hex bug (`correction-2` \S1) silently dropped from every check
   before this generation script -- specifically included so that population
   is no longer entirely unread.

## Group 1: original 20-site sample

### 1. pydantic-2.13.4 pydantic/_internal/_validators.py::less_than_or_equal_validator row 304 -> pydantic/_internal/_validators.py::_safe_repr
```python
def less_than_or_equal_validator(x: Any, le: Any) -> Any:
    try:
        if not (x <= le):
            raise PydanticKnownError('less_than_equal', {'le': _safe_repr(le)})
        return x
    except TypeError:
        raise TypeError(f"Unable to apply constraint 'le' to supplied value {x}")
```

### 2. pydantic-2.13.4 pydantic/_internal/_schema_gather.py::traverse_schema row 129 -> pydantic/_internal/_schema_gather.py::traverse_schema
```python
            traverse_schema(v, context)
    elif schema_type == 'chain':
        for step in schema['steps']:
            traverse_schema(step, context)
    elif schema_type == 'lax-or-strict':
        traverse_schema(schema['lax_schema'], context)
        traverse_schema(schema['strict_schema'], context)
```

### 3. pydantic-2.13.4 pydantic/_internal/_schema_gather.py::traverse_schema row 184 -> pydantic/_internal/_schema_gather.py::traverse_schema
```python
        if 'schema' in schema:
            traverse_schema(schema['schema'], context)
        if 'json_schema_input_schema' in schema:
            traverse_schema(schema['json_schema_input_schema'], context)
    else:
        if 'schema' in schema:
            traverse_schema(schema['schema'], context)
```

### 4. pydantic-2.13.4 pydantic/plugin/_schema_validator.py::build_wrapper row 103 -> pydantic/plugin/_schema_validator.py::filter_handlers
```python
        return func
    else:
        on_enters = tuple(h.on_enter for h in event_handlers if filter_handlers(h, 'on_enter'))
        on_successes = tuple(h.on_success for h in event_handlers if filter_handlers(h, 'on_success'))
        on_errors = tuple(h.on_error for h in event_handlers if filter_handlers(h, 'on_error'))
        on_exceptions = tuple(h.on_exception for h in event_handlers if filter_handlers(h, 'on_exception'))

```

### 5. pydantic-2.13.4 pydantic/_internal/_schema_gather.py::traverse_schema row 126 -> pydantic/_internal/_schema_gather.py::traverse_schema
```python
            traverse_schema(choice, context)
    elif schema_type == 'tagged-union':
        for v in schema['choices'].values():
            traverse_schema(v, context)
    elif schema_type == 'chain':
        for step in schema['steps']:
            traverse_schema(step, context)
```

### 6. pydantic-2.13.4 tests/benchmarks/generate_north_star_data.py::person row 118 -> tests/benchmarks/generate_north_star_data.py::date_string
```python
        'id': f.uuid4(),
        'name': f.name(),
        'height': str(f.pydecimal(min_value=1, max_value=2, right_digits=2)),
        'entry_created_date': date_string(),
        'entry_created_time': lax_time(),
        'entry_updated_at': lax_datetime(),
        'websites': list_of(website, max_length=5),
```

### 7. pydantic-2.13.4 tests/test_datetime.py::test_datetime row 171 -> tests/test_datetime.py::create_tz
```python
        ('2012-04-23T10:20:30.400+02:30', datetime(2012, 4, 23, 10, 20, 30, 400_000, create_tz(150))),
        ('2012-04-23T10:20:30.400+02:00', datetime(2012, 4, 23, 10, 20, 30, 400_000, create_tz(120))),
        ('2012-04-23T10:20:30.400-02:00', datetime(2012, 4, 23, 10, 20, 30, 400_000, create_tz(-120))),
        (b'2012-04-23T10:20:30.400-02:00', datetime(2012, 4, 23, 10, 20, 30, 400_000, create_tz(-120))),
        (datetime(2017, 5, 5), datetime(2017, 5, 5)),
        (0, datetime(1970, 1, 1, 0, 0, 0, tzinfo=timezone.utc)),
        # Numeric inputs as strings
```

### 8. pydantic-2.13.4 pydantic/_internal/_fields.py::collect_model_fields row 422 -> pydantic/_internal/_fields.py::_update_fields_from_docstrings
```python
            update_field_from_config(config_wrapper, ann_name, field_info)

    if config_wrapper.use_attribute_docstrings:
        _update_fields_from_docstrings(cls, fields)

    pydantic_extra_info: PydanticExtraInfo | None = None
    if '__pydantic_extra__' in type_hints:
```

### 9. click-8.4.1 tests/test_shell_completion.py::test_argument_default row 137 -> tests/test_shell_completion.py::_get_words
```python
            Argument(["b"], type=Choice(["b"]), default="b"),
        ],
    )
    assert _get_words(cli, [], "") == ["a"]
    assert _get_words(cli, ["a"], "b") == ["b"]
    # ignore type validation
    assert _get_words(cli, ["x"], "b") == ["b"]
```

### 10. click-8.4.1 tests/test_shell_completion.py::test_argument_nargs row 281 -> tests/test_shell_completion.py::_get_words
```python
            Option(["-z"]),
        ],
    )
    assert _get_words(cli, [], "") == ["a", "b"]
    assert _get_words(cli, ["a"], "") == ["a", "b"]
    assert _get_words(cli, ["a", "b"], "") == ["c", "d"]
    assert _get_words(cli, ["a", "b", "c"], "") == ["c", "d"]
```

### 11. pydantic-2.13.4 tests/test_forward_ref.py::test_undefined_types_warning_raised_by_usage row 983 -> tests/test_forward_ref.py::pytest_raises_user_error_for_undefined_type
```python


def test_undefined_types_warning_raised_by_usage(create_module):
    with pytest_raises_user_error_for_undefined_type('Foobar', 'UndefinedType'):

        @create_module
        def module():
```

### 12. pydantic-2.13.4 pydantic/_internal/_fields.py::rebuild_model_fields row 466 -> pydantic/_internal/_fields.py::update_field_from_config
```python
                new_field = _recreate_field_info(
                    field_info, ns_resolver=ns_resolver, typevars_map=typevars_map, lenient=False
                )
                update_field_from_config(config_wrapper, f_name, new_field)
                rebuilt_fields[f_name] = new_field

        if cls.__pydantic_extra_info__ is not None and not cls.__pydantic_extra_info__.complete:
```

### 13. pydantic-2.13.4 tests/test_forward_ref.py::test_undefined_types_warning_1a_raised_by_default_2b_forward_ref row 930 -> tests/test_forward_ref.py::pytest_raises_user_error_for_undefined_type
```python


def test_undefined_types_warning_1a_raised_by_default_2b_forward_ref(create_module):
    with pytest_raises_user_error_for_undefined_type(defining_class_name='Foobar', missing_type_name='UndefinedType'):

        @create_module
        def module():
```

### 14. pydantic-2.13.4 tests/test_type_hints.py::test_parent_sub_model row 137 -> tests/test_type_hints.py::inspect_type_hints
```python


def test_parent_sub_model(ParentModel):
    inspect_type_hints(ParentModel, None, DEPRECATED_MODEL_MEMBERS)


def test_root_model_as_field():
```

### 15. click-8.4.1 tests/test_shell_completion.py::test_chained row 97 -> tests/test_shell_completion.py::_get_words
```python
        ],
    )
    assert _get_words(cli, [], "") == ["get", "set", "start"]
    assert _get_words(cli, [], "s") == ["set", "start"]
    assert _get_words(cli, ["set", "start"], "") == ["get"]
    # subcommands and parent subcommands
    assert _get_words(cli, ["get"], "") == ["full", "set", "start"]
```

### 16. pydantic-2.13.4 tests/test_datetime.py::test_datetime row 137 -> tests/test_datetime.py::create_tz
```python
        ('11:05:00Z', time(11, 5, 0, tzinfo=timezone.utc)),
        ('11:05:00+00:00', time(11, 5, 0, tzinfo=timezone.utc)),
        ('11:05-06:00', time(11, 5, 0, tzinfo=create_tz(-360))),
        ('11:05+06:00', time(11, 5, 0, tzinfo=create_tz(360))),
        ('11:05:00-25:00', Err('Input should be in a valid time format, timezone offset must be less than 24 hours')),
    ],
)
```

### 17. click-8.4.1 tests/test_shell_completion.py::test_command row 35 -> tests/test_shell_completion.py::_get_words
```python
    assert _get_words(cli, [], "") == []
    assert _get_words(cli, [], "-") == ["-t", "--test", "--help"]
    assert _get_words(cli, [], "--") == ["--test", "--help"]
    assert _get_words(cli, [], "--t") == ["--test"]
    # -t has been seen, so --test isn't suggested
    assert _get_words(cli, ["-t", "a"], "-") == ["--help"]

```

### 18. click-8.4.1 tests/test_shell_completion.py::test_chained row 101 -> tests/test_shell_completion.py::_get_words
```python
    assert _get_words(cli, ["set", "start"], "") == ["get"]
    # subcommands and parent subcommands
    assert _get_words(cli, ["get"], "") == ["full", "set", "start"]
    assert _get_words(cli, ["get", "full"], "") == ["set", "start"]
    assert _get_words(cli, ["get"], "s") == ["set", "start"]


```

### 19. pydantic-2.13.4 pydantic/_internal/_typing_extra.py::get_model_type_hints row 367 -> pydantic/_internal/_typing_extra.py::try_eval_type
```python
                    else:
                        globalns, localns = ns_resolver.types_namespace

                        hints[name] = try_eval_type(value, globalns, localns)

    return hints

```

### 20. pydantic-2.13.4 pydantic/_internal/_schema_gather.py::traverse_definition_ref row 87 -> pydantic/_internal/_schema_gather.py::traverse_schema
```python
        ctx.collected_references[schema_ref] = def_ref_schema
        traverse_schema(definition, ctx)
        if 'serialization' in def_ref_schema:
            traverse_schema(def_ref_schema['serialization'], ctx)
        traverse_metadata(def_ref_schema, ctx)
    else:
        # The `'definition-ref'` schema was already encountered, meaning
```

## Group 2: one site per previously-unresolved target

### R1. click-8.4.1 tests/test_termui.py::test_progress_bar_update_min_steps row 342 -> tests/test_termui.py::_create_progress
```python


def test_progress_bar_update_min_steps(runner):
    bar = _create_progress(update_min_steps=5)
    bar.update(3)
    assert bar._completed_intervals == 3
    assert bar.pos == 0
```

### R2. pydantic-2.13.4 pydantic/_internal/_fields.py::collect_model_fields row 306 -> pydantic/_internal/_fields.py::is_valid_field_name
```python
            # Then `assigned_value` would be the method, even though no default was specified:
            assigned_value = PydanticUndefined

        if not is_valid_field_name(ann_name):
            continue
        if cls.__pydantic_root_model__ and ann_name != 'root':
            raise NameError(
```

### R3. pydantic-2.13.4 tests/test_model_signature.py::_equals row 19 -> tests/test_model_signature.py::_equals
```python
    if isinstance(a, str) and isinstance(b, str):
        return a.replace(' ', '') == b.replace(' ', '')
    elif isinstance(a, Iterable) and isinstance(b, Iterable):
        return all(_equals(a_, b_) for a_, b_ in zip(a, b))
    else:
        raise TypeError(f'arguments must be both strings or both lists, not {type(a)}, {type(b)}')

```

### R4. pydantic-2.13.4 tests/benchmarks/generate_north_star_data.py::blog row 81 -> tests/benchmarks/generate_north_star_data.py::lax_int
```python
    return {
        'type': 'blog',
        'title': f.text(max_nb_chars=40),
        'post_count': lax_int(),
        'readers': lax_int(),
        'avg_post_rating': lax_float(min_value=0, max_value=5),
        'url': f.url(),
```

### R5. pydantic-2.13.4 tests/test_pickle.py::test_pickle row 238 -> tests/test_pickle.py::dataclass_factory
```python
        (ImportableChildDataclass, False),
        (ImportableChildDataclass, True),
        # Locally-defined Pydantic dataclass can only be pickled with cloudpickle.
        pytest.param(dataclass_factory(), True, marks=cloudpickle_pypy_xfail),
        (child_dataclass_factory(), True),
        # Pydantic dataclass generated from builtin can only be pickled with cloudpickle.
        pytest.param(pydantic.dataclasses.dataclass(ImportableBuiltinDataclass), True, marks=cloudpickle_pypy_xfail),
```

### R6. pydantic-2.13.4 tests/benchmarks/test_north_star.py::north_star_data_bytes row 69 -> tests/benchmarks/test_north_star.py::_north_star_data_bytes
```python

@pytest.fixture(scope='module')
def north_star_data_bytes():
    return _north_star_data_bytes()


def _north_star_data_bytes() -> bytes:
```

### R7. pydantic-2.13.4 tests/test_pickle.py::test_pickle row 243 -> tests/test_pickle.py::builtin_dataclass_factory
```python
        # Pydantic dataclass generated from builtin can only be pickled with cloudpickle.
        pytest.param(pydantic.dataclasses.dataclass(ImportableBuiltinDataclass), True, marks=cloudpickle_pypy_xfail),
        # Pydantic dataclass generated from locally-defined builtin can only be pickled with cloudpickle.
        pytest.param(pydantic.dataclasses.dataclass(builtin_dataclass_factory()), True, marks=cloudpickle_pypy_xfail),
    ],
)
def test_pickle_dataclass(dataclass_type: type, use_cloudpickle: bool):
```

### R8. requests-2.34.2 src/requests/compat.py::compat row 48 -> src/requests/compat.py::_resolve_char_detection
```python
    return chardet


chardet = _resolve_char_detection()

# -------
# Pythons
```

## Totals

Group 1: 20 of 20 original-sample sites re-read. Group 2: 8 previously-unresolved-target sites read.
