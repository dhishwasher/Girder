# Agent instructions

See `CLAUDE.md`'s "Agent tooling: use Bit Code's own CLI" section for the
token-discipline and Bit Code tooling instructions previously here — moved
there so they carry project-instruction weight rather than sitting in this
file.
